{
  "name": "hearts-online-animal-island",
  "version": "1.4.20",
  "description": "红心大战联机版：4位易记房间号、离线提示、房间超时自动解散",
  "main": "server.js",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "start": "node server.js",
    "check": "vue-tsc --noEmit && node --check server.js && node --check src/server/config.js && node --check src/server/staticFiles.js && node --check src/server/realtimeGame.js"
  },
  "dependencies": {
    "pinia": "^3.0.4",
    "sass": "^1.101.0",
    "vite-plugin-pwa": "^1.3.0",
    "vue": "^3.5.38",
    "workbox-window": "^7.4.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.7",
    "@vue/tsconfig": "^0.9.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vue-tsc": "^3.3.5"
  }
}
